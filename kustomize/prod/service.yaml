apiVersion: v1
kind: Service
metadata:
  name: community-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8083
      protocol: TCP
      targetPort: http
